{
  "name": "screeps-play",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "node -e \"require('fs').rmSync('temp-dist', { recursive: true, force: true }); require('fs').rmSync('dist', { recursive: true, force: true });\"",
    "build:ts": "tsc -p tsconfig.json --outDir temp-dist",
    "build:flatten": "node tools/flatten-dist.js temp-dist dist",
    "build": "pnpm run build:ts && pnpm run build:flatten",
    "build:upload": "pnpm run build && node tools/upload-screeps.js",
    "start": "node tools/watch-and-flatten.js",
    "start:upload": "node tools/watch-and-upload.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.17.0",
  "dependencies": {
    "dotenv": "^16.4.5",
    "https-proxy-agent": "^7.0.4",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/screeps": "^3.3.8",
    "chokidar": "^5.0.0"
  }
}
